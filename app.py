from flask import Flask, request, jsonify\nfrom flask_cors import CORS\nfrom dotenv import load_dotenv\nimport os\nimport ast\nfrom error_detector import AIReviewer\nfrom ai_suggestor import get_ai_suggestion, validate_code\n\n# Load environment variables\nload_dotenv()\n\napp = Flask(__name__)\nCORS(app)\n\n# ============================================\n# Health Check Endpoint\n# ============================================\n@app.route('/api/health', methods=['GET'])\ndef health_check():\n    return jsonify({"status": "healthy", "message": "AI Code Reviewer API is running"}), 200\n\n# ============================================\n# Code Review Endpoint\n# ============================================\n@app.route('/api/review', methods=['POST'])\ndef review_code():\n    try:\n        data = request.get_json()\n        \n        if not data or 'code' not in data:\n            return jsonify({"error": "No code provided"}), 400\n        \n        code_string = data['code']\n        \n        # Validate syntax\n        is_valid, syntax_error = validate_code(code_string)\n        \n        # Get AI suggestions using LangChain\n        from langchain_groq import ChatGroq\n        from langchain_core.prompts import PromptTemplate\n        \n        model = ChatGroq(\n            model="llama-3.1-8b-instant",\n            groq_api_key=os.getenv("GROQ_API_KEY")\n        )\n        \n        if not is_valid:\n            formatted_prompt = f"""\nThe following Python code contains a syntax error:\n\n{code_string}\n\nPython Error Message:\n{syntax_error}\n\nExplain:\n1. What the error means\n2. Why it occurred\n3. Provide corrected code\n4. Time & Space complexity of corrected code\n"""\n        else:\n            prompt_template = PromptTemplate(\n                input_variables=["code_string"],\n                template="""\nYou are an experienced Python coding teacher.\n\nAnalyze the following Python code and provide:\n\n1. Code quality suggestions\n2. Any logical errors\n3. Time complexity\n4. Space complexity\n5. Improvements for readability\n\nCode:\n{code_string}\n"""\n            )\n            formatted_prompt = prompt_template.format(code_string=code_string)\n        \n        result = model.invoke(formatted_prompt)\n        ai_review = result.content\n        \n        # Get AST-based analysis\n        try:\n            tree = ast.parse(code_string)\n            reviewer = AIReviewer(code_string)\n            reviewer.visit(tree)\n            reviewer.analyze()\n            \n            style_violations = reviewer.violations\n            style_score = max(reviewer.score, 0)\n        except Exception as e:\n            style_violations = [str(e)]\n            style_score = 0\n        \n        return jsonify({\n            "success": True,\n            "syntax_valid": is_valid,\n            "syntax_error": syntax_error,\n            "ai_review": ai_review,\n            "style_violations": style_violations,\n            "style_score": style_score\n        }), 200        \n    except Exception as e:\n        return jsonify({"error": str(e)}), 500\n\n# ============================================\n# Batch Review Endpoint\n# ============================================\n@app.route('/api/batch-review', methods=['POST'])\ndef batch_review():\n    try:\n        data = request.get_json()\n        \n        if not data or 'files' not in data:\n            return jsonify({"error": "No files provided"}), 400\n        \n        files = data['files']\n        results = []\n        \n        for file_data in files:\n            file_name = file_data.get('name', 'unknown')\n            code_string = file_data.get('code', '')\n            \n            is_valid, syntax_error = validate_code(code_string)\n            \n            try:\n                tree = ast.parse(code_string)\n                reviewer = AIReviewer(code_string)\n                reviewer.visit(tree)\n                reviewer.analyze()\n                \n                results.append({\n                    "file_name": file_name,\n                    "syntax_valid": is_valid,\n                    "syntax_error": syntax_error,\n                    "style_violations": reviewer.violations,\n                    "style_score": max(reviewer.score, 0)\n                })\n            except Exception as e:\n                results.append({\n                    "file_name": file_name,\n                    "syntax_valid": False,\n                    "syntax_error": str(e),\n                    "style_violations": [],\n                    "style_score": 0\n                })\n        \n        return jsonify({\n            "success": True,\n            "results": results,\n            "total_files": len(results)\n        }), 200        \n    except Exception as e:\n        return jsonify({"error": str(e)}), 500\n\n# ============================================\n# Main Entry Point\n# ============================================\nif __name__ == '__main__':\n    app.run(debug=True, host='0.0.0.0', port=5000)\n